import * as employees from './employees';
import * as employee from './employee';
import * as tasks from './tasks';
import * as task from './task';

export const API = {
  employees, tasks, employee, task,
};
